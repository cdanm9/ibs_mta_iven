<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m"
	xmlns:l="sap.ui.layout">
	<!-- <l:VerticalLayout >
		<Text text="Grndetails" />
		
	</l:VerticalLayout> -->
<Table id = "demoTable" items="{/GRNDetailsSet}">
		
		<columns>
        <Column><Text text="Material Doc" /></Column>
			<Column><Text text="Received Date" /></Column>
			<Column><Text text="Plant" /></Column>
			<Column><Text text="Movement Type" /></Column>
            <Column><Text text="Material" /></Column>
			<Column><Text text="PO Quantity" /></Column>
			<Column><Text text="Received Qty" /></Column>
		</columns>
		<items>
			<ColumnListItem >
				<cells>
					<Text text="{Mblnr}"/>
					<Text text="{Budat}"
						/>
					<Text
						text="{Plname}" />
                    <Text
						text="{Btext}" />
                     <Text
						text="{Maktx}" />
					<Text
						text="{Meins}" />
					<Text
						text="{Bstmg}" />
					

				</cells>
			</ColumnListItem>
		</items>
	</Table>




</core:FragmentDefinition>
